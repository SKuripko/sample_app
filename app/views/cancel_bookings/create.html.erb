<% provide(:title, 'Cancel booking') %>
<div class="row">
	<%= render 'shared/sidebar_form' %>	
	<div class="col-md-9">
	  <%= render 'shared/cancel_booking_form' %>
    <div id="js"><%= JSON.pretty_generate(@results) %></div>
	</div>
</div>