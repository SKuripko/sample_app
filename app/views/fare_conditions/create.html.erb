<% provide(:title, 'Fare condition') %>
<div class="row">
	<%= render 'shared/sidebar_form' %>	
	<div class="col-md-9">
	  <%= render 'shared/fare_condition_form' %>
    <div id="js"><%= JSON.pretty_generate(@results) %></div>
	</div>
</div>